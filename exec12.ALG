algoritmo "semnome"
var
          VI,VF,T: real
inicio

      Escreval("Digite o valor do produto em reais")
      Leia(VI)
      VF <- ((5/100) * VI)
      T <- (VI-VF)
      Escreval("O valor promocional será de :" ,T," REAIS")

fimalgoritmo